Single-dominant no-separator benchmark

What changed:
- Added a low-latency benchmark mode that skips ConvTasNet and treats each 2s analysis window as one dominant speaker stream.
- Kept speaker tracking based on the window’s voice features plus recent direction history.
- Wired the mode through the fixed 30s benchmark and summary output as `single_dominant_no_separator`.
- Added smoke coverage for the new benchmark path.

Validation:
- `PYTHONPATH=. pytest -q tests/test_realtime_pipeline_integration_mock.py -k 'fixed_speaker_scene_generator_emits_expected_metadata or fixed_speaker_benchmark_smoke_with_mock_separation or fixed_speaker_benchmark_target_first_identified_smoke or fixed_speaker_benchmark_single_dominant_no_separator_smoke'`

Run command:
- `PYTHONPATH=. python -m realtime_pipeline.fixed_speaker_benchmark --out-dir realtime_pipeline/output/fixed_speaker_benchmark_noise0p0x_no_separator --noise-scale 0.0 --identity-backend mfcc_legacy --single-dominant-no-separator`
